<!-- GENERATED by scripts/render_results.py. Edit the generator, not this page. -->

# Early stopping

### The first early-stopping measurement (issue #306)

One cell, 4M rows by 100 columns of synthetic regression, 9 arms on one L40S, and the two quantities the [protocol](../benchmark-protocol.md#early-stopping) keeps apart: what it costs a library to score a validation set every round, and how long it takes to reach a model that stopped on its own. The eval rows come off the train side, 10 percent of it, so every arm below fits 3.6M rows and the held-out test split stays untouched.

#### Per-round eval overhead

Each arm fits those rows twice at 100 fixed rounds, once with a validation set attached and once without, with no patience armed either time. Nothing but the eval set differs between the two fits, so the ratio of their `fit_s` values is what scoring a validation set every round costs that library.

bonsai's device growers pay the most of anything measured here: +10.0% for CUDA depthwise and +7.3% for CUDA leafwise, against +1.8% for XGBoost-GPU and +3.2% for LightGBM-CUDA, with CatBoost-GPU between them at +7.7%. bonsai's own CPU depthwise reads -2.1%, so the cost belongs to the device plane and not to eval work as such. Issue #326 is the finding's home: the per-round eval predicts on the host, which charges a device-resident fit a host round trip every round, and both fix candidates (a device-side eval predict, or scoring every k rounds) want sizing measurements before any code.

Two of the four CPU arms read below zero, -2.1% and -1.3%, and an attached eval set cannot make a fit faster; that is what puts the noise floor of this measurement near 2 percent, and all four CPU arms sit inside it. The device readings do not. Pairing the repeats by index instead of taking best of repeats on each side, bonsai's CUDA depthwise spans +10.0% to +17.2% while XGBoost-GPU never exceeds +3.3%, so the ordering survives every pairing even where the digit moves.

Fit seconds without and with the eval set, best of repeats (GPU arms 2, CPU arms 1); the cheapest device arm is bolded, and the CPU arms are left unbolded because a reading inside the noise floor is not a win.

| arm | off fit_s | eval fit_s | eval overhead |
|---|---|---|---|
| bonsai cuda dw | 2.90s | 3.19s | +10.0% |
| bonsai cuda lw | 4.67s | 5.02s | +7.3% |
| xgb cuda | 5.87s | 5.97s | **+1.8%** |
| lgbm cuda | 11.01s | 11.36s | +3.2% |
| catboost gpu | 5.40s | 5.82s | +7.7% |
| bonsai cpu dw | 30.54s | 29.89s | -2.1% |
| xgb hist | 22.63s | 23.33s | +3.1% |
| lgbm cpu | 37.05s | 36.58s | -1.3% |
| catboost cpu | 29.42s | 29.89s | +1.6% |

#### Time to stop

Patience 50, an iteration cap of 2000 that the validation set rather than the cap is meant to end, learning rate 0.05. This is the latency of a retrain nobody has hand-tuned the round count for, so the wall clock includes the patience rounds each library grew and then discarded.

XGBoost-GPU reaches a stopped model fastest, 20.9s against bonsai CUDA depthwise's 23.7s, which inverts the fixed-iteration ordering of the same cell above (2.9s against 5.9s at 100 rounds). The two readings agree: bonsai's lead at this shape is ingest (0.4s against 3.9s), a run of roughly a thousand rounds amortizes ingest to nothing, and bonsai's per-round device cost sits above XGBoost's here (24.3ms against 16.6ms, eval overhead included). The overhead reading above is part of that gap rather than separate from it.

6 of the 9 arms stop between 900 and 966 rounds and return the same test r2 to four decimals, 0.8965: six implementations of one stopping rule landing on one model. LightGBM-CUDA stops earliest at 648 rounds and gives up a little accuracy for it (0.8958). CatBoost runs longest on both planes (1384 and 1371 rounds) and returns the best metric (0.8997), but it scores with its own objective's eval metric and runs without a leaf-size floor, so its extra rounds are not a like-for-like reading of the same rule. The plane gap is the largest spread on the page: 21s to 50s on the GPU against 152s to 331s on the CPU for the same stopped model.

Fit seconds to a stopped model, retained rounds, and the test metric of the model stopping actually produced; fastest fit and best metric bolded.

| arm | fit_s | stopped_at | test r2 |
|---|---|---|---|
| bonsai cuda dw | 23.7s | 913 | 0.8965 |
| bonsai cuda lw | 38.7s | 918 | 0.8965 |
| xgb cuda | **20.9s** | 966 | 0.8965 |
| lgbm cuda | 50.2s | 648 | 0.8958 |
| catboost gpu | 31.7s | 1384 | **0.8997** |
| bonsai cpu dw | 226.3s | 950 | 0.8965 |
| xgb hist | 152.2s | 941 | 0.8965 |
| lgbm cpu | 220.5s | 900 | 0.8965 |
| catboost cpu | 330.8s | 1371 | 0.8996 |

Three protocol caveats this section inherits. `stopped_at` is the retained round count rather than a best-iteration index, because the four libraries number that index two different ways. XGBoost is the only arm that does not truncate on a stop, so the runner passes `iteration_range` explicitly; read without it, the metric would describe the overshot model. CatBoost shrinks its model to the best iteration whenever an eval set is present, detector or no detector, so the fixed-iteration arm pins `use_best_model=False`, or its metric would describe a shorter model than the one whose fit was timed.

*Source: [`early-stop-4M-2026-08.jsonl`](../../../benchmarks/results/early-stop-4M-2026-08.jsonl). One pod (L40S, 2026-08-04), SCALING knobs, spec [early-stop-4M.json](../../../python/bonsai/bench/specs/early-stop-4M.json) run as `python -m bonsai.bench run --spec early-stop-4M`, measured on the tail of the standings-refresh rental; evidence for issue #306, and the eval-overhead reading is issue #326. Measured at `05290e0` (2026-08-04, pod-NVIDIA-L40S).*
