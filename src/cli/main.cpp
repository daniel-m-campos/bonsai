#include <iostream>

int main(int /*argc*/, char * /*argv*/[])
{
    // TODO(user): wire CLI11 + subcommand handlers (fit, predict, eval,
    // bench, info). Spec: docs/architecture/9-cli.md (TBD).
    std::cout << "bonsai: not yet implemented\n";
    return 0;
}
